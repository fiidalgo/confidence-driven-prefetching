TEST_TOOL_ROOTS := cdp
TEST_ROOTS :=
TOOL_ROOTS :=
SA_TOOL_ROOTS :=
APP_ROOTS :=
OBJECT_ROOTS :=
DLL_ROOTS :=
LIB_ROOTS :=
RUNNABLE_TESTS := $(TEST_TOOL_ROOTS) $(TEST_ROOTS)
SANITY_SUBSET := $(TEST_TOOL_ROOTS) $(TEST_ROOTS)

$(OBJDIR)insmix$(PINTOOL_SUFFIX): $(OBJDIR)insmix$(OBJ_SUFFIX) $(CONTROLLERLIB)
	$(LINKER) $(TOOL_LDFLAGS) $(LINK_EXE)$@ $^ $(TOOL_LPATHS) $(TOOL_LIBS)
